aiogram>=3.3,<4
python-dotenv>=1.0
python-docx>=1.1
requests>=2.31
pdfplumber>=0.11
numpy>=1.23
pytest>=7.0

# OpenAI-совместимый клиент (Polza использует SDK OpenAI)
openai>=1.52,<2

# Работа с изображениями (даунскейл/сжатие/кодирование в base64)
Pillow>=10.0

# Извлечение изображений из PDF (PyMuPDF)
pymupdf>=1.24

# DOC -> DOCX без MS Office (опционально)
aspose-words>=24.6

# DOCX -> PDF фолбэк (опционально; требует MS Word / Word for Mac)
docx2pdf>=0.1.8; sys_platform == "win32" or sys_platform == "darwin"

# Только для Windows (MS Word COM, опционально)
pywin32; sys_platform == "win32"
